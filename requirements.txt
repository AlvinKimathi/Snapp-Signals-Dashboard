
# Requirements for the Snapp Signals Dashboard
#
# This file lists all Python packages needed to run the Snapp Signals
# dashboard and associated API.  It includes the core visualization and
# web frameworks (Streamlit and FastAPI), data processing libraries
# (pandas, matplotlib, plotly), the OpenAI client library for the bot,
# and utilities like requests for HTTP calls.  Versions are pinned to
# reasonable minimums to ensure compatibility with Python 3.11+ and the
# codebase as of March 2026.  You can adjust the version ranges as
# needed for your environment.

streamlit>=1.30,<2.0
pandas>=2.1,<3.0
numpy>=1.26,<2.0
plotly>=5.18,<6.0
matplotlib>=3.8,<4.0
requests>=2.31,<3.0
openai>=1.13,<2.0

# FastAPI and related packages
fastapi>=0.135,<1.0
uvicorn[standard]>=0.25,<1.0
pydantic>=2.6,<3.0

# Optional: load environment variables from a .env file
python-dotenv>=1.0,<2.0